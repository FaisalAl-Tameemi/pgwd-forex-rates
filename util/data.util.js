'use strict';

const request = require('request');

/**
 * [findRate finds the permutations of all the rates between the symbols provided]
 * @param  {[array]} symbols [The symbols of the rates to fetch]
 * @return {[array]}         [The rates of the symbols provided]
 */
const findRates = (symbols) => {
	// TODO: implement
}

module.exports = {
	findRates
};
